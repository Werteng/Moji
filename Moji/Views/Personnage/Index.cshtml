@model IEnumerable<Moji.Entities.Personnage>

@{
    ViewData["Title"] = "Index";
}



<div class="col-12 ">
    <p>
        <div>
            <a class="creerPerso" asp-action="Create">Créer un nouveau personnage</a>
        </div>
    </p>
    <table class="table">
        <thead>
            <tr>
                <th class="text-center">
                    @Html.DisplayNameFor(model => model.Nom)
                </th>
                <th class="text-center">
                    @Html.DisplayNameFor(model => model.Prenom)
                </th>

                <th class="text-center">
                    @Html.DisplayNameFor(model => model.Or)
                </th>
                <th class="text-center">
                    @Html.DisplayNameFor(model => model.SanteMax)
                </th>
                <th class="text-center">
                    @Html.DisplayNameFor(model => model.ManaMax)
                </th>

                <th class="text-center">
                    @Html.DisplayNameFor(model => model.Niveau)
                </th>
                <th>
                    Race
                </th>
                <th class="text-center">
                    Classe
                </th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.Nom)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Prenom)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Or)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.SanteMax)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.ManaMax)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Niveau)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Race.Libelle)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Classe.Libelle)
                    </td>
                    <td>
                            <a asp-action="Edit" class="btn btn-success mb-2 bouttonCrePerso" asp-route-id="@item.Id">Editer</a>
                            <a asp-action="Details" class="btn btn-info mb-2 bouttonCrePerso" asp-route-id="@item.Id">Details</a>
                            <a asp-action="DeletePage" class="btn btn-danger mb-2 bouttonCrePerso" asp-route-id="@item.Id">Supprimer</a>
                        
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>
<div class="clear">

</div>
